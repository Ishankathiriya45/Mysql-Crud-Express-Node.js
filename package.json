{
  "name": "project_03_ecommerce",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon bin/www",
    "migrate-local:all": "sequelize db:migrate --env local",
    "migrate-test:all": "sequelize db:migrate --env test",
    "migrate-prod:all": "sequelize db:migrate --env production",
    "migrate-local:undo:one": "sequelize-cli db:migrate:undo --env local --name",
    "migrate-test:undo:one": "sequelize-cli db:migrate:undo --env test --name",
    "migrate-prod:undo:one": "sequelize-cli db:migrate:undo --env production --name",
    "seed-local:all": "sequelize db:seed:all --env local",
    "seed-test:all": "sequelize db:seed:all --env test",
    "seed-prod:all": "sequelize db:seed:all --env production",
    "seed-local:seed:one": "sequelize db:seed --env local",
    "seed-test:seed:one": "sequelize db:seed --env test",
    "seed-prod:seed:one": "sequelize db:seed --env production"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cli-color": "^2.0.4",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.0",
    "fs": "^0.0.1-security",
    "joi": "^17.13.3",
    "jsonwebtoken": "^9.0.2",
    "moment": "^2.30.1",
    "morgan": "^1.10.0",
    "morgan-body": "^2.6.9",
    "multer": "^1.4.5-lts.2",
    "mysql2": "^3.14.0",
    "nodemailer": "^7.0.3",
    "path": "^0.12.7",
    "sequelize": "^6.37.7",
    "sequelize-cli": "^6.6.2",
    "swagger-autogen": "^2.23.7",
    "swagger-ui-express": "^5.0.1",
    "winston": "^3.17.0",
    "winston-daily-rotate-file": "^5.0.0"
  }
}
